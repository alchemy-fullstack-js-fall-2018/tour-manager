require('dotnev').config();
const Tour = require('../../lib/models/tour');
require('../../lib/util/connect')();

describe('tour model', () => {

    it('creates a tour in my db', () => {
        
    });

});
